using System;

namespace lib.common
{
    /*public class AsyncWorker<T> : IDisposable
    {
        private AsyncProcessor<T, Func<dynamic>> underylingProcessor;
        
        public AsyncWorker(String name, Consum)

        public void Dispose()
        {
            underylingProcessor?.Dispose();
        }
    }*/
}